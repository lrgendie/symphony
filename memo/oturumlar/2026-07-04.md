# Oturum Günlüğü — 2026-07-04 (Oturum 10)

## Yapılanlar

**Faz 3 ilk dikey dilim: agent motoru** — SPEC-AGENT.md birebir uygulandı.

- `packages/core/src/agent/`: errors, jail (workspace hapsi), permissions
  (deny>allow>varsayılan), definition (frontmatter + varsayılan coder),
  tools (6 araç: diff üretimi, sır maskeleme, temiz env, yıkıcı komut sezgiseli),
  engine (AI SDK v7 tool-calling döngüsü; izin TEK kapı; maxSteps; AGENT_TOOL_LOOP;
  bayat diff; iptal; durum makinesi).
- SQLite v3: `agent_runs` + `agent_steps`; açılışta AGENT_DAEMON_RESTART işaretleme.
- Daemon: agent.start/cancel, permission.respond, agents.list; snapshot'ta koşular +
  bekleyen izinler; `testProviders` enjeksiyonu (yalnız test).
- Protokol: `agents.list`/`agents.list.ok` + `AgentSummary` (önce PROTOKOL.md, kural 1 ✓).
- Provider arayüzü: `languageModel()` + `forwardsTemperature` (4 adapter refaktörü).
- CLI: `symphony agents`, `symphony agent <ad> "<görev>"` (renkli diff, e/d/h izin sorusu).
- Yeni bağımlılıklar (envantere işlendi): execa, tinyglobby, picomatch, diff.
- Testler: jail/permissions/definition/tools/engine + daemon-agent WS entegrasyonu.
  **115/115 yeşil; build + lint temiz.** ROADmap Faz 3 kabul testinin ilk üç kriteri kanıtlı.

## Oturum ekonomisi + devir hazırlığı (kullanıcı talebi)

- `memo/BAGLAM.md` yaratıldı: dosya haritası + "görev → ne okumalı" tablosu +
  token bütçesi kuralları. CLAUDE.md oturum rutinine bağlandı (geniş tarama yasak).
- `memo/DEVIR.md` yaratıldı: Fable → Opus devir talimatı (rol, disiplin, Faz 3
  kalanları, teknik tuzak haritası). ROADMAP başına devir notu eklendi.
- DURUM.md yalınlaştırıldı: yalnız aktif faz ayrıntılı, geçmiş fazlar özet.

## Öğrenilen dersler (DEVIR.md'ye de işlendi)

- AI SDK v7: system mesajı `messages` içinde yasak → `generateText({ instructions })`.
- AI SDK v7: geçersiz araç çağrısı fırlatmıyor; `invalid: true` + SDK'nın kendi
  error tool-result'ı. `MockLanguageModelV3` ile deterministik motor testi mümkün.
- TS2883: pnpm'de adapter dönüş tipleri açık yazılmalı (`Promise<LanguageModel>`).
- Windows: better-sqlite3 açıkken silinmez (testte close), symlink testi junction ile.

## Sonraki oturum

DURUM.md "Sıradaki adım": ① qwen3:8b ile canlı agent doğrulaması ② MCP istemcisi
③ `symphony add` ④ TUI agent modu.
