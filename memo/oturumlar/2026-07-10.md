# Oturum Günlüğü — 2026-07-10 (Sonnet → Fable devri; tek uzun oturum)

## Yapılanlar (kronolojik)

1. **Dilim M2 — hafıza REST+CLI+TUI yüzeyi (Sonnet), BİTTİ + canlı doğrulandı.**
   `GET/PUT /api/memory` (Bearer) · `MemoryGet/PutSchema` (shared/rest.ts) ·
   `readProfileSnapshot`/`writeProfile` (profile.ts — loadProfile'dan AYRI amaç: TAM içerik,
   kesmez) · `symphony memory` / `memory path` · TUI "🧠 profil aktif (N karakter)" satırı.
   Canlı: curl 401/GET/PUT roundtrip + CLI komutları gerçek daemon'a karşı; TUI satırı
   kullanıcı tarafından görsel teyit. 255→263 test.

2. **Canlı bulgu #1 — profil kimlik karışıklığı (Sonnet).** qwen3:8b, "ben kimim?" sorusuna
   profildeki adı KENDİ kimliği sanarak cevap verdi. Düzeltme: `formatProfileContext` (tek
   kaynak, agent+chat ortak) — "SENİN kimliğin DEĞİL, kullanıcıya ait bağlam" açık uyarısı.
   263→264 test. Yan gözlem: qwen3:8b "10 kıta yaz" isteğinde ~15dk GPU %98/87°C döngüye
   girdi (temperature:0 + uzun yapılandırılmış üretim şüphesi); kullanıcı süreci sonlandırdı.
   `maxOutputTokens` tavanı yok — olası ayrı dilim, kapsam dışı bırakıldı (DURUM'da not).

3. **Dilim M3 — arşiv damıtma (Sonnet), BİTTİ + canlı doğrulandı.** `damitici` üçüncü default
   agent (salt-okur) · `symphony memory distill <dizin> [--bulut]`: mtime-sıralı dosya listesi
   görev metnine, yerel model AÇIKÇA pinlenir (--bulut yoksa; yerel yoksa hata), sonuç
   `profil.taslak.md`'ye — canlı profile yazma yolu YOK. 264→273 test.

4. **Canlı bulgu #2 — damıtıcı kirlenmesi (Sonnet).** İlk canlı damıtmada taslağa arşivde hiç
   geçmeyen, kullanıcının GERÇEK profilinden gelen cümleler sızdı — M1 enjeksiyonu damitici'ye
   de uygulanıyordu. Düzeltme: `damitici` profil enjeksiyonundan muaf. İkinci canlı koşu TEMİZ
   taslak üretti; canlı profil bayt-bayt değişmedi. 273→274 test. **Gün sonu taban: 274/274.**

5. **Faz 5 tasarımı (Fable, model devri sonrası) — ADR-014.** Kullanıcı Faz 5'i (çoklu agent
   orkestrasyonu) seçti ve tasarımın pahalı modelde yapılmasını istedi. Sonnet'in 5 açık sorusu
   karara bağlandı: devretme = motor-içi dinamik `run_agent` aracı (host-orkestrasyon ve yeni
   protokol mesajı REDDEDİLDİ — gerekçeler ADR'de); hiyerarşi `parentRunId?` ADDITIVE; çocuk
   izinleri AYNI kapıdan kullanıcıya; risk sınıfı hedefe göre dinamik; derinlik-1 +
   MAX_CHILD_RUNS=8 sigortaları; maliyet v1 = şef prompt'u + kural-tabanlı router; varsayılan
   "sef" agent'ı. PROTOKOL.md/SPEC-AGENT.md (§9 YENİ) "planlandı — O1" işaretli; ROADMAP Faz 5
   güncellendi ("agent tanımları dosya olarak" fiilen Faz 3'te bitmişti, işaretlendi).
   **Dilim planı O1 (çekirdek devretme) → O2 (sef + CLI/TUI hiyerarşi) → O3 (masaüstü +
   ROADMAP kapanışı) DURUM.md'de, Sonnet'e hazır.**

## Dersler

- Küçük yerel model, system bağlamındaki örtük ayrımları (profil ≠ kimlik) kaçırıyor —
  enjekte edilen her blok, sahibini AÇIKÇA söylemeli.
- "Her koşuya enjekte" varsayılanı, bağlamı TEMİZ okuması gereken agent'larda (damıtıcı)
  kendi kendini kirletme döngüsü yaratır — enjeksiyon istisnaları tasarım kararıdır.
- M3'ün host-orkestrasyon deseni damıtma için doğruydu ama Faz 5 şefi için yanlış: plan
  sabitse host koşturur, planın kendisi zekânın çıktısıysa devretme motorda olmalı (ADR-014).

## Yarına devir

Sıradaki iş: **Dilim O1** (DURUM.md'de adım adım; model: Sonnet). Daemon şu an M3 build'iyle
çalışıyor (restart edilmiş durumda). Yerel model genişletmesi (qwen2.5-coder:7b + qwen2.5vl:7b)
konuşuldu, kurulmadı — kullanıcı isteyince `ollama pull` yeterli.

## Ek (aynı gün, devam eden oturum)

6. **Faz 5 canlı doğrulama + canlı bulgu #3 (Sonnet):** kullanıcının gerçek sef denemesi
   router'ın yeni kurulan qwen2.5vl'yi (vision) metin görevlerinde yanlışlıkla seçtiğini
   ortaya çıkardı → `preferTextCapable()` filtresi (+2 test). run_agent kodu temiz çıktı.
7. **Faz 4 senkron + otomatik açılış + "hangi dosya" (Sonnet):** ROADMAP Faz 4 gerçek duruma
   getirildi; `desktop.autoLaunch` + `desktop-launch.ts`; `runFiles` zengin dosya önizlemesi
   (diff izin kartı kapansa da kalır). 288→297 test.
8. **ADR-015 (Fable):** proje görünümü + yol haritası tasarımı — proje=cwd-türevi (kayıtsız),
   v1 yalnız canlı gruplama, roadmap=sözleşmeli markdown parser + `GET /api/roadmap`,
   adım-koşu bağlama v2'ye, görsel=panel (graf Faz 6 Bağlam Haritası'nın). Dilimler P1/P2/P3
   DURUM'da, uygulama Sonnet'te.
9. **Dilimler P1/P2/P3 (Sonnet):** ADR-015 uygulandı — canlı proje gruplaması, roadmap parser +
   `GET /api/roadmap`, masaüstü roadmap paneli. 297→315 test. **Faz 4 tamamen kapandı.**
10. **Kaçak üretim sigortası (Opus):** canlı bulgu #1'in açık bıraktığı riski kapattı —
    agent/sohbet turlarında çıktı-token tavanı YOKTU. `config.limits.maxOutputTokens` (vars.
    8192, tek varsayılan kaynağı) + agent frontmatter `maxOutputTokens?` ezmesi (`.optional()`,
    default değil) + `engine.ts` her `streamText` turuna iletir; `finishReason:"length"` →
    `AGENT_MAX_OUTPUT_TOKENS` ile failed (usage ÖNCE kaydedilir — token harcandı, defterde
    görünmeli). Sohbet yolunda `options.maxTokens ?? config` geri düşüşü. SPEC-AGENT §4 +
    PROTOKOL.md güncellendi. 315→**320** test.
    - **Gerçek sağlayıcıya karşı doğrulandı** (mock değil, izole script — rapor §5.4 dersi):
      Ollama `maxOutputTokens:16` → `finishReason:"length"`, `outputTokens=16` (harfiyen uyuyor);
      tavansız → `"stop"`, 463 token. Yan gözlem: 16 token'ın tamamı qwen3'ün `<think>`
      muhakemesine gitti (`chars=0`) — muhakemeli modellerde çok düşük tavan boş cevap üretir.
    - **Bilinçle YAPILMADI:** duvar-saati zaman aşımı — Dilim O1'in "AbortSignal ham `streamText`
      tüketimini kesmiyor" bulgusu hâlâ açık, zaman aşımı bugün çalışmayabilir. Ayrı dilim.
      *(Aynı gün, madde 11'de bu gerekçe çürütüldü — zaman aşımı artık engelsiz.)*
11. **İptal teşhisi (Opus): O1'in AbortSignal şüphesi ÇÜRÜTÜLDÜ.** Üç ölçüm, hepsi gerçek
    sağlayıcıya/daemon'a karşı: ham `fetch` abort → 3ms `AbortError` (Ollama HTTP iptalini
    onurlandırıyor); AI SDK `streamText` abort → `for await` döngüsü **2ms**'de çıkıyor, 41
    karakterde kesiliyor (abortsuz taban 35sn/1689 karakter); canlı daemon'da akış ortasında
    `agent.cancel` → `cancelled` **5ms**. Yani Esc/Ctrl+C baştan beri çalışıyormuş.
    - **Kök neden:** `engine.test.ts`'teki `deferredStream()` mock'u `abortSignal`'ı hiç
      dinlemiyordu (gerçekte sinyal `fetch`'e iner, bağlantı kopar). O1 mock'un sınırını
      motorun sınırı sandı. `engine.ts`'in `catch`'i zaten doğruydu: `run.abort.signal.aborted`
      kontrolü ÖNCE geldiği için reddeden `await result.response` koşuyu `cancelled` yapıyor.
    - **Yapılan:** mock artık sinyali dinliyor (abort → akışı AbortError'la hatalandırır);
      O1'de "imkânsız" sanılan senaryo için YENİ regresyon testi (akan tur cancel → `cancelled`,
      completed/failed DEĞİL, akan metin kaybolmaz). 320→**321** test; build/test/lint temiz.
      DURUM.md + BAGLAM.md'deki yanlış kayıt ÇÜRÜTÜLDÜ diye işaretlendi (sonraki model miras almasın).
    - **Ders (BAGLAM'a da yazıldı):** mock'un sınırını üretimin sınırı sanma; şüpheyi gerçek
      sağlayıcıya sor — rapor §5.4'ün aynı dersi, ikinci kez.
12. **ADR-016 (Fable): Faz 6 Zeka Katmanı tasarımı.** Kullanıcı sıralamayı bağladı (önce
    router zekası, sonra harita; açık geri bildirim dahil). Kararlar: skor = sorgu-zamanı
    agregasyon (fiziksel tablo yok; cancelled hariç; hız `requests`'ten — koşu süresi insan
    beklemesi içerir); router v2 = kural iskeleti + skor düzeltmesi (MIN_SAMPLES=3, Laplace +
    feedback 2× ağır, yalnız demote/promote + kanıtlı reason — protokolsüz); hafıza maddesi
    ADR-013 ile kapandı (`[x]`); `feedback.submit` + göç v5 (Z2); deterministik `symphony
    report` + REST (Z3, LLM yok, lokallik testli); bağlam haritası = mevcut verinin grafı
    (embedding YOK, kenarlar deterministik, d3-force 2D ayrı görünüm; Z4 veri + Z5 görünüm).
    PROTOKOL'e planlı işaretler kondu, ROADMAP maddeleri bağlandı, dilim talimatları DURUM'da —
    uygulama Sonnet'te (Z1 sıradaki).
