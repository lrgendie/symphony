# Oturum 1 devamı — 2026-07-03 (gece)

## Yapılanlar

1. GitHub remote bağlandı: `lrgendie/symphony`; dal `master → main`; geçmişler birleştirilip push'landı.
2. Temperature varsayılanı 0 ilkesi ROADMAP'e işlendi.
3. VS C++ dosyaları (`symphony.vcxproj`, `.slnx` vb.) kullanıcı onayıyla silindi.
   Not: `.vs/` önbelleği diskte kilitli (VS açık) — gitignore'da, VS kapanınca silinebilir.
4. **"Fable mirası" stratejisi:** kullanıcı ileride ekonomik nedenle daha küçük modele geçecek.
   İlke: pahalı zekâ kararlara, ucuz zekâ yazıma. Bu kapsamda yazılanlar:
   - `CLAUDE.md` — proje anayasası (dokunulmaz kurallar, kod kuralları, belge haritası)
   - `docs/PROTOKOL.md` — WS/REST protokol spesifikasyonu v1 (zarf, mesaj katalogları,
     agent durum makinesi, snapshot tabanlı yeniden bağlanma, port 7770, token auth)
   - `docs/SPEC-AGENT.md` — agent motoru şartnamesi (araç risk sınıfları, izin kuralları,
     workspace jail, diff önizleme, güvenlik değişmezleri)
   - `docs/kararlar/KARARLAR.md` — 11 ADR (daemon mimarisi, TS, Vercel AI SDK, Tauri,
     Ollama, SQLite, MCP, temp=0, pnpm, keychain, snapshot>replay)
   - ROADMAP: her faza (0–8) ölçülebilir **kabul testi** eklendi
5. README belge haritası güncellendi.

## Model geçiş planı (karar)

- Fable varken: tasarım/spec/protokol/zor çekirdek işleri.
- Sonrası günlük iş: Sonnet 5; zor hata avında Opus 4.8.
- Fable cımbızla: faz geçişlerinde tasarım incelemesi oturumu.

## Faz 0 tamamlandı (aynı gece)

- pnpm 11.9.0 kuruldu (Node 24.14.1 zaten vardı).
- Monorepo: kök yapılandırmalar (package.json, pnpm-workspace, turbo, tsconfig.base,
  eslint flat config, prettier, vitest) + `.github/workflows/ci.yml`.
- **`@symphony/shared`:** PROTOKOL.md'nin tamamı zod'a kodlandı — zarf + parseMessage/
  createMessage, 11 istek şeması, 26 olay/cevap şeması, agent durum makinesi
  (VALID_TRANSITIONS + canTransition). Temperature=0 varsayılanı şemada zorlanıyor.
- **`@symphony/core`:** `~/.symphony` paths modülü (SYMPHONY_HOME desteği, ensureSymphonyHome).
- **`@symphony/cli`:** çalışan iskelet (`node dist/index.js` → banner).
- `ui`/`desktop`: Faz 4 yer tutucuları.
- Sonuç: build ✓, 24/24 test ✓, lint ✓, format ✓ — Faz 0 kabul testi yerelde geçti.
- Çözülen pürüzler: TS 6'da `@types/node` için `"types": ["node"]` gerekiyor;
  shared'da `crypto` için ortam-bağımsız globals.d.ts; prettier markdown'dan uzak tutuldu.

## Oturum 2 — Faz 1 ilk dikey dilim (aynı gün)

- CI'ın yeşil olduğu kullanıcı tarafından doğrulandı → Faz 0 resmen kapandı.
- claude-api referansı yüklendi. **Kritik bulgu:** Claude 4.7+ (Opus 4.8, Sonnet 5)
  `temperature` parametresini 400 ile REDDEDIYOR → Anthropic adapter'ı göndermez
  (ADR-008 diğer sağlayıcılar için geçerli). Fiyat tablosu koda işlendi (pricing.ts).
- `@symphony/core` Faz 1 modülleri: config.ts, secret-store.ts (Keyring+Env+Composite),
  set-key.ts, providers/{types,pricing,anthropic}.ts, server/{token,bus,daemon}.ts, main.ts.
- Daemon: REST /api/health (authsuz) + /api/chat (SSE, Bearer token) + WS /ws
  (hello→token→snapshot; chat.start/cancel, models.list, providers.status, state.sync).
- Bağımlılıklar: fastify 5, ws 8, ai v7, @ai-sdk/anthropic v4, @napi-rs/keyring, pino 10.
  pnpm 11 build-script onayı: pnpm-workspace.yaml → allowBuilds.
- 37/37 test yeşil (daemon WS entegrasyon testi dahil); build+lint+format temiz.
- Daemon canlı doğrulandı: `GET /api/health` → `{ok:true, protocolVersion:1}`.

## Oturum 3 — İlk canlı Claude cevabı 🎉

- Kullanıcı için `anahtar-kaydet.bat` yazıldı (çift tıkla → yapıştır → Enter; kopyalama
  derdi yok). Anahtar keychain'e kaydedildi, doğrulandı.
- Pürüz 1: önceki oturumdan kalan daemon port 7770'i tutuyordu; yeni kopya EADDRINUSE ile
  çökerken token dosyasının üstüne yazdı → eski daemon'a erişim kilitlendi. Süreç öldürülüp
  temiz başlatıldı. **Ders: tek-kopya kilidi gerekli (DURUM'a işlendi).**
- Pürüz 2: Git Bash'te Türkçe karakterli JSON body Content-Length uyuşmazlığı verdi →
  gövde dosyadan gönderildi (`--data-binary @file`).
- **CANLI TEST GEÇTİ:** curl → daemon → Claude Opus 4.8 streaming cevap.
  Kullanım: 62 giriş / 99 çıkış tokeni, maliyet $0.002785 — sayaçlar gerçek veriyle çalıştı.

## Oturum 4 — SQLite veri katmanı + hata telemetrisi

- `packages/core/src/db/store.ts`: DataStore (better-sqlite3 v12, WAL,
  `PRAGMA user_version` ile sıralı göçler). Tablolar: `requests` (her isteğin meta
  kaydı: sağlayıcı, model, süre, token, maliyet, durum ok/error/cancelled, hata kodu)
  ve `telemetry` (scope, kod, mesaj, stack, context=girdi ÖZETİ JSON — ham içerik yok,
  SPEC-AGENT §7).
- Daemon bağlantısı: `runChat` başarı/hata/iptal fark etmeksizin `requests`'e yazar;
  iptal DEĞİLSE telemetriye de düşer (iptal kullanıcı eylemi, hata değil).
  Beklenmeyen WS hataları `ws.message` scope'uyla telemetriye gider.
- `usage.updated.totals` artık SQLite'tan kalıcı kümülatif (sağlayıcı+model bazında);
  daemon restart'ında sıfırlanmaz. `usage.query` WS mesajı uygulandı (protokolde zaten
  tanımlıydı): from/to aralığı + provider/model/day gruplaması.
- pnpm notu: better-sqlite3 build izni `pnpm-workspace.yaml → allowBuilds`'te zaten
  hazırdı; prebuild indi, VS C++ derleyicisi gerekmedi.
- Testler: store birim testleri (6) + daemon entegrasyon testi (başarısız istek kaydı,
  telemetri, usage.query, ham içerik sızmıyor). **44/44 yeşil; build+lint+format temiz.**

## Oturum 5 — Ollama adapter'ı (kod tamam, canlı test bekliyor)

- **Paket kararı:** topluluk `ollama-ai-provider@1.2` (AI SDK v4 dönemi) ve
  `ollama-ai-provider-v2@3.6` (peer: ai ^5||^6, zod ^4) bizim ai v7 + zod v3 ile
  uyumsuz → resmî `@ai-sdk/openai-compatible@3.0.3` seçildi (provider spec v4,
  ai v7 ile aynı). Ollama'nın OpenAI-uyumlu `/v1` ucu kullanılıyor.
  GEREKSINIMLER.md envanteri güncellendi; ADR gerekmedi (ADR-003/005 değişmedi).
- `providers/ollama.ts`: dinamik model listesi (`GET /api/tags` → ModelInfo,
  local:true), isConfigured = sunucu erişilebilirliği (2sn zaman aşımı, hata
  fırlatmaz — sunucu yoksa boş liste/false), streamChat maliyet 0.
  Temperature Ollama'ya İLETİLİR (ADR-008; Anthropic'te iletilmiyordu).
- `ProviderAdapter.listModels` sync → **async** (dinamik listeler için);
  Anthropic + daemon models.list (Promise.all) uyarlandı.
- Daemon'a `ollama` provider'ı kayıtlı; `DaemonOptions.ollamaBaseUrl` test için.
- Testler: sahte Ollama HTTP sunucusu (node:http, SSE chunk'ları OpenAI
  formatında) — liste, erişilemezlik, streaming+usage+temperature doğrulanıyor.
  CI'da gerçek Ollama gerekmez. **47/47 yeşil; build+lint+format temiz.**
- Kullanıcı Ollama kuruyor (masaüstü kurulum = CLI + otomatik sunucu);
  bu oturumda 11434 henüz cevap vermiyordu.

## Oturum 5 devamı — Ollama CANLI kabul testi GEÇTİ 🎉

- Kullanıcı Ollama masaüstü uygulamasını kurdu; sunucu 11434'te ayağa kalktı
  (`ollama` CLI eski shell'in PATH'inde görünmedi — API'den ilerlendi, sorun değil).
- `qwen3:8b` API'den indirildi (`POST /api/pull`, ~5.2 GB, birkaç dakika).
  Model: 8.2B, Q4_K_M, 40k bağlam, tools+thinking yetenekli — 8 GB VRAM'e uygun.
- Port 7770 boş doğrulandı (EADDRINUSE dersi) → daemon başlatıldı →
  curl → `/api/chat` → **yerel modelden Türkçe streaming cevap**:
  19 giriş / 330 çıkış token (thinking dahil), maliyet $0, süre ~55 sn
  (ilk yükleme VRAM'e model taşımayı içeriyor).
- SQLite doğrulandı: `requests` tablosunda `ollama/qwen3:8b/ok` satırı düştü.
- **Dönüm noktası: "hem yerel hem bulut" 1. ayda kanıtlandı** — Claude Opus 4.8
  (bulut, ücretli) ve qwen3:8b (yerel, ücretsiz) aynı daemon'dan, aynı protokolle akıyor.
- Daemon test sonrası düzgün kapatıldı (port temiz bırakıldı).

## Oturum 6 — Tek-kopya kilidi + Router v1

- **Tek-kopya kilidi:** `issueDaemonToken` ikiye bölündü (`generateDaemonToken` +
  `persistDaemonToken`); token dosyası ancak `app.listen` başarılı olunca yazılıyor.
  Açılışta porta sağlık sondası: çalışan symphonyd varsa `DAEMON_ALREADY_RUNNING`
  ile temiz hata (main.ts dostane mesaj basıp exit 1). Test: ikinci kopya reddediliyor,
  token dosyası bayt bayt aynı kalıyor, çalışan daemon sağlıklı.
- **Router v1** (`packages/core/src/router/`): kelime-kümesi tabanlı görev sınıflama
  (kod/hızlı/uzunBağlam/genel), VRAM tespiti (nvidia-smi → yoksa null),
  kalite sıralaması (opus>sonnet>haiku), yerel "coder" varyant tercihi,
  preferLocal/maxCostUsd kısıtları, tahmini maliyet (2k giriş+1k çıkış varsayımı).
  Yalnız KULLANILABİLİR sağlayıcılardan öneri (isConfigured filtresi).
  Hiç model yoksa `PROVIDER_NONE_AVAILABLE` hatası (şema min 1 öneri ister).
- **Ders:** JS regex `\b` ASCII'dir — "özet" gibi Türkçe karakterli kelimede
  sınır bulamıyor; kelime-tam eşleşmeye geçildi.
- Daemon testleri artık sahte Ollama'ya bağlanıyor (CI'da deterministik).
- **57/57 test yeşil; build+lint+format temiz.**

## Oturum 7 — OpenAI + Google adapter'ları (anahtarsız kod, kullanıcı onayıyla)

- `providers/openai.ts` (gpt-5.1, gpt-5-mini, gpt-5-nano; 400k bağlam) ve
  `providers/google.ts` (gemini-2.5-pro/flash; 1M bağlam). Fiyatlar pricing.ts'e
  işlendi (2026-06 katalog önbelleği).
- **GPT-5 ailesi de temperature kabul etmiyor** (akıl yürüten modeller, Claude 4.7+
  gibi 400 döner) → iletilmiyor; Gemini destekliyor → ADR-008 gereği iletiliyor.
- Daemon artık 4 sağlayıcı kaydediyor; router yalnız yapılandırılmışları önerir.
- Testler in-memory SecretStore ile (gerçek keychain'e/env'e dokunmaz):
  isConfigured, listModels, anahtarsız PROVIDER_NOT_CONFIGURED, fiyat hesabı.
- Zincirleme düzeltme: eski test "openai"yi bilinmeyen sağlayıcı sanıyordu →
  "yok-boyle-saglayici" yapıldı. Sağlamlık: daemon.close() artık açık WS
  soketlerini terminate ediyor (yoksa app.close sonsuz bekleyebiliyordu).
- **61/61 test yeşil; build+lint+format temiz. Faz 1 kod olarak tamam.**

- **Kabul koşusu geçti:** `~/.symphony` + repo grep'lendi (sk-ant-/sk-proj-/AIzaSy
  desenleri) → diskte anahtar YOK (tek eşleşme set-key.ts'teki yer tutucu yorum).
  Anahtarlar yalnız keychain'de — CLAUDE.md kural 3 doğrulandı.

## Oturum 8 (akşam) — Faz 2 CLI: ilk dikey dilim TAMAM

- Bağımlılıklar: ink 7 + react 19 + commander 15 + chalk 5 + ink-text-input;
  cli artık core'a da bağımlı (paths/config + daemon spawn hedefi).
- **Daemon istemcisi**: WS+hello el sıkışması, replyTo korelasyonu, istek
  zaman aşımı (30sn), üstel geri çekilmeli yeniden bağlanma (PROTOKOL §6),
  olay aboneliği, `chat()` akış sarmalayıcı.
- **İki incelikli hata testlerle yakalandı ve düzeltildi:**
  1. chat.start.ok + error aynı TCP paketinde gelince korelasyon yarışı
     kaybediliyordu → kayıt GÖNDERMEDEN ÖNCE + sticky (ilk cevapta silinmez);
     ayrıca dinleyicisiz "error" olayı EventEmitter'da çökme yaratıyordu → koruma.
  2. core'un exports haritası require.resolve'a kapalıydı → `"./daemon"` alt yolu
     + `"default"` koşulu eklendi; spawn hedefi `@symphony/core/daemon`.
- **Otomatik başlatma kabul koşusu GEÇTİ**: port 7770 boşken `symphony status`
  daemon'ı spawn etti; sağlayıcı sağlığı doğru (anthropic+ollama up, diğerleri down);
  SQLite kalıcı kullanım geçmişi restart sonrası da göründü (349 token, $0).
- `symphony models`: 4 sağlayıcı / 9 model (yerel qwen3:8b dahil) listelendi.
- TUI: ModelPicker + Chat + App; picker ink-testing-library ile testli
  (ok tuşu ESC dizisi kaynakta \u001B olarak — ham kontrol karakteri dersi).
- TS dersi: MESSAGE_PAYLOAD_SCHEMAS (REQUEST ∪ EVENT) üzerinde generic indeksleme
  çözülemiyor → istemcide tek noktada tip silme (createMessage runtime'da zaten
  şema doğruluyor).
- **69/69 test yeşil; build+lint+format temiz.**

## Oturum 8 sonu — TUI CANLI DENEME GEÇTİ ✅

- Kullanıcı TUI'yi çalıştırdı: model seçici → qwen3:8b → streaming sohbet AKTI.
  Faz 2 kabul testinin "model seçici + streaming sohbet" ayakları canlı doğrulandı.
- Kullanıcı sohbette "klasörü taşı" istedi; model sadece anlattı → beklenen davranış:
  şu an araçsız saf sohbet var, dosyaya dokunan agent motoru Faz 3'ün işi
  (SPEC-AGENT.md). Kullanıcıya fark anlatıldı; Faz 3 beklentisi netleşti.
  Not: qwen3:8b `tools` yetenekli → Faz 3'te yerel agent modeli olabilir.

## Oturum 9 — Faz 2 KAPANDI: global kurulum + watch + sohbet geçmişi

- **Global kurulum:** `pnpm setup` (PNPM_HOME kayıt defterine yazıldı; kullanıcı
  YENİ terminal açmalı) + `pnpm add -g link:packages/cli`. Ders: pnpm 11'de
  `pnpm link --global` kaldırılmış; npm i -g workspace:* çözemez → link: protokolü.
  `symphony --version` PATH'ten çalıştı; symlink olduğu için build sonrası hep güncel.
- **`symphony watch`:** daemon olay akışını canlı basar (chat.delta/completed +
  usage.updated; Faz 3'te agent.* katılacak). `attachWatchOutput` çıktıyı enjekte
  alır → testli.
- **Eş zamanlılık kabul testi GEÇTİ** (concurrency.test.ts): iki DaemonClient,
  A'nın başlattığı sohbetin delta+completed olaylarını B aynı sessionId ile aldı;
  abonelikten çıkınca yazım duruyor.
- **Sohbet geçmişi (dikey dilim):** PROTOKOL.md'ye §1.1 REST katalog + §3
  oturum/replace kuralı yazıldı → shared'a `protocol/rest.ts` (History* şemaları)
  → store göç v2 (`sessions`+`messages`, PK(session_id,idx), FK CASCADE,
  foreign_keys pragma) → daemon runChat başarıda `saveChatTurn` (replace,
  idempotent; değişmeyen mesajın `at`'i korunur; iptal/hata geçmişi değiştirmez)
  → REST `GET /api/history/sessions[?limit]` + `/:id` (404, 401) → CLI
  `symphony history [id-ön-eki]` (shared şemayla parse — kural 1) → TUI sabit
  sessionId (useRef) → çok tur tek oturum (chat.test.tsx ile testli).
- **Canlı doğrulama:** curl → daemon → qwen3:8b turu (sabit sessionId, $0);
  `symphony history` listede gösterdi, ön ekle tam döküm geldi.
- **76/76 test yeşil; build+lint+format temiz. ROADMAP Faz 2 kabul testi kapandı.**

## Oturum 9 devamı — kullanıcı eklemeleri: kimlik + vizyon maddeleri

Kullanıcı üç istek ekledi; roadmap'e işlendi + ilki hemen kodlandı:

1. **Karşılama ekranı (Faz 2.5 — KODLANDI):** `symphony` açılışında logo + sürüm/
   protokol + Türkçe tarih + sağlayıcı durumu (●/○) + SQLite kullanım özeti +
   kısayol ipuçları. Logo TEK modülde (`tui/logo.ts`): kullanıcının HAZIR LOGOSU VAR,
   paylaşınca yalnız o dosya değişecek (geçici: Calvin S figlet "SYMPHONY").
   `welcome.tsx` bileşeni testli (ders: ink satır sonu boşluklarını kırpar → trimEnd).
2. **CLI→masaüstü otomatik açılış (Faz 4'e eklendi):** `symphony` başlayınca masaüstü
   uygulaması da açılır; `config.json → desktop.autoLaunch` (varsayılan açık).
   "Yaşayan sistem" hissi zaten Faz 4 Living Interface hedefi.
3. **PDF mimari rehberi (Faz 7'ye eklendi):** sistem ne yapar, kod haritası, araçlar/
   agent'lar/skill'ler kodda nerede, protokol özeti, komut başvurusu. Kaynak
   `docs/REHBER.md` (Faz 4 sonunda iskelet, kodla birlikte büyür) → PDF'e derlenir.

77/77 test yeşil; build+lint+format temiz.

## Sonraki oturum

- **Faz 3: agent motoru** — `docs/SPEC-AGENT.md` mutlaka okunarak: araç seti,
  agent döngüsü (AI SDK tool-calling), izin sistemi + diff önizleme, MCP istemcisi.
- ~~Kullanıcı logosu gelince `tui/logo.ts` güncelle~~ → GELDİ ve uyarlandı:
  tesseract + nöral sinaps teması; logo.ts segment-renkli yapıya geçti
  (satır içi çoklu renk: cyan iskelet, magenta iç küp, redBright sinaps ◉).
  Marka paleti cyan/magenta/red — Faz 4 Living Interface bu paletle kurulmalı.
- OpenAI/Google canlı doğrulama (anahtar gelirse).
