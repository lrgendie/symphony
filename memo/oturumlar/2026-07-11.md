# Oturum Günlüğü — 2026-07-11 (Sonnet gece/gündüz + Fable tasarım; tek uzun oturum devamı)

## Yapılanlar (kronolojik)

- **CI düzeltmesi doğrulandı:** dün geceki `windows-latest` geçişi yeşil — Linux'ta `node:path`
  POSIX davranışı yüzünden günlerdir kırık olan CI kapandı.
- **F3 canlı kabulü:** `ensureDesktopRunning()` gerçek home'a karşı çalıştırıldı — kurulu
  `%LOCALAPPDATA%\Symphony\app.exe` bulunup doğrudan başlatıldı (PID teyitli). F3 tamamen bitti.
- **F4 `symphony sync`:** beyaz liste + git akışı, gerçek git ile (yerel bare repo = uzak,
  ağ yok) uçtan uca testli. İki bulgu: `.gitignore` kendini de negatiflemeli; `simpleGit`
  named import olmalı. 399→409 test.
- **F5 `symphony update`/`rollback`:** `POST /api/shutdown` (PROTOKOL'e işlendi) + versions.json
  + execa'yla npm'e delege; adanmış daemon örneğiyle shutdown kanıtlandı. 409→418 test.
- **F6 release matrix:** `.github/workflows/release.yml` (tag v* → test → 4 platform tauri-action
  bundle [draft release] + npm publish). Gerçek tag BİLİNÇLE atılmadı (kullanıcı tetiklemeli).
- **F7 REHBER:** `docs/REHBER.md` yazıldı + `md-to-pdf` (puppeteer onayı kullanıcıyla) +
  `pnpm docs:pdf` çalıştı, PDF kullanıcıya gönderildi. **Faz 7 F1-F7 tamamen kapandı.**
- **Fable — Faz 8 tasarım oturumu:** **ADR-018 yazıldı** (KARARLAR.md), dilimler D1-D6
  DURUM.md'de. Ana fikir: Faz 8 yeni motor inşa etmez — mevcut agent motoru kendi reposuna
  çevrilir (doktor = agent tanımı, sandbox = git worktree, canlıya alma = F5 restart zinciri,
  watchdog = denetimli apply). Zemin ölçümü: daemon gerçekten repo'dan (`tsx`) çalışıyor.

## Dersler

- CI'nin platformu, projenin GERÇEK doğrulama platformuyla eşleşmeli — aksi görünmez kırıklık.
- Gerçek-git testlerinde repo'ya test-yerel `user.name/email` kur; tam paket koşusunda 5sn
  timeout yetmeyebilir.
- pnpm supply-chain: build script'li bağımlılık (`puppeteer`) `approve-builds` ister —
  kullanıcı onayı alınmalı, `pnpm-workspace.yaml.allowBuilds`'e işlenir.

## Yarına devir

- Faz 7 kullanıcı-tetiklemeli kalanlar: npm login/org (F2), NPM_TOKEN + gerçek tag (F6),
  .msi/yönetici doğrulaması (F3, opsiyonel).
- Faz 8 uygulaması D1'den başlar (Sonnet) — talimat DURUM.md'de.

## Ek oturum (Fable, akşam) — Bağlam Haritası v2 tasarımı (ADR-019)

- Kullanıcı v2 vizyonunu anlattı: küratörlü harita ("bunu haritaya ekle" → başlık+tarihli
  kalıcı düğüm), grup kur/kopar/bağla, her LLM için düğüm (yerel/API ayrımı), agent→koşu→model
  üçlüsü, **haftalık otomatik tek düğüme katlanma** (kenara yerleşir, tarihsel takip).
- Görsel katman kullanıcıya soruldu → **Yaşayan 2D** seçildi (tam-3D reddedildi).
- **ADR-019 yazıldı** (7 karar; ADR-016 Karar 6'nın "model bağı kenar değil" reddi revize —
  katlanma çöp-graf riskini kaldırıyor; katlanma SORGU-ZAMANI kuralı, göç yalnız kürasyon
  için v6). Dilimler H1..H5 adım adım DURUM.md'de; TASARIM.md §3'e görsel dil notu düştü.
- **Karar 7 (kullanıcı sorusu üzerine): paketleme/güncelleme etkileri incelendi** — göç v6
  SALT EKLEMELİ şartı (rollback F5 güvenliği; `migrate()` eski kodda yeni sürümü yok sayar,
  store.ts'ten kanıtlandı), eski istemcinin katı enum kırılganlığı (H2'de UI toleransı),
  yeni-UI/eski-daemon zarif hatası (H3), H3/H5 için yeni desktop installer gerekliliği
  (ui/dist gömülü), sync'in kürasyonu bilinçli taşımaması, REHBER.md güncellemesi (H4).
- **Mimari tarama TAMAMLANDI** → `rapor/mimari-tarama-2026-07-11.md`: 617/617 yeşil, yasak
  desenler sıfır; bulgular — B1 (ORTA: pollBekci korumasız G/Ç + bozuk bekci.json daemon'ı
  düşürebilir), B2 (ORTA: patch apply merge çakışması yarım-merge bırakır), B3 (belge-test-kod
  uyuşmazlığı: "bozuk JSON'a dayanıklı" iddiası yanlış — test niyeti doğruluyor), B4-B6 düşük,
  N1-N6 uyumluluk notları (Türkçe tanımlayıcılar kararı kullanıcıya). Tarama sırasında ayrıca
  ADR-019'daki göç numarası v6→v7 düzeltildi (v6 = ADR-018 patches; BAGLAM bayattı).

## Ek oturum (Fable, akşam) — zanaat rehberi

- Kullanıcının isteğiyle YENİ `zanaat/` klasörü + `zanaat/OPERASYON-REHBERI.md` yazıldı:
  Fable→Opus devri için kural DEĞİL çalışma-biçimi katmanı (8 alışkanlık: isteği kelimelerin
  altından okuma, doğrulanabilir parçalara bölme, riski konumlama, yeniden türeterek doğrulama,
  bilinen/tahmin etiketi, kendi sonucuna saldırı, cevap→gerekçe→risk sırası, sahte-yetkinlik
  hataları kataloğu). Projenin gerçek yara izleri (cache şema bug'ı, worktree ata-repo sızması,
  2026-07-09 mükerrer Dilim 1) örnek olarak gömüldü. `memo/DEVIR.md` girişine işaret eklendi.
  Kod/test değişikliği YOK.

## Ek oturum (Fable, gece) — B1+B3 düzeltmesi + Dilim H1 (kısmen)

- **B1+B3 BİTTİ ve testli:** `trust.ts`/`registry.ts`'in `readTrust`/`readBekciRegistry`'si
  artık sentaks-bozuk JSON'da (eskiden fırlıyordu) çökmeden boşa düşüyor; `daemon.ts`
  `pollBekci` iki katmanlı hata yutma aldı (registry okuma + proje-başına try/catch) — elle
  bozulmuş bir `bekci.json` artık daemon'ı düşüremiyor. 617→620 test.
- **Dilim H1 (kürasyon temeli) KISMEN:** PROTOKOL+shared+store(göç v7)+`curation.ts`(SAF,
  testli)+daemon'a 8 handler kodlandı, 620→644 test — ama daemon.ts'in yeni case'lerini
  WS üzerinden egzersiz eden entegrasyon testi (talimattaki roundtrip+koruma reddi+restart
  kalıcılığı) kullanıcının "limit doldu" uyarısı üzerine YAZILAMADAN oturum kapandı.
  Devralan oturum ÖNCE bunu tamamlamalı (ayrıntı DURUM.md'de, "BİLİNÇLİ EKSİK" bölümü).
