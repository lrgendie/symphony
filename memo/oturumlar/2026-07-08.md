# Oturum 14 — 2026-07-08 (Fable)

## Dilim 8: Yaşayan Küre → YAŞAYAN TESSERACT

Kullanıcı iki yeni referans görsel verdi (`Tasarım/görsel1.png`, `görsel2.png`: bakır/bronz
dış küp, cyan+mor sinaps iç ağı, parlak kırmızı çekirdek) ve R3F geliştirme promptu yazdı;
"tasarım yeteneklerine güveniyorum, şaşırt beni" diyerek serbest geliştirme yetkisi verdi.
Küre topolojisi tamamen emekli edildi; dashboard'un merkezi artık markanın kendisi: canlı tesseract.

### Yapılanlar

1. **TASARIM.md revizyonu (görsel anayasa):** §1 palete `--copper #c9803f` + `--violet #a78bfa`
   (index.css'e de eklendi; logo cyan/magenta kalır, bakır yalnız sahnenin donanım düzlemi).
   §2 baştan yazıldı: topoloji, malzeme düzlemleri, durum eşlemesi, converge salvosu, atım
   sistemi, yumuşatma ilkeleri. Küre + vektörel dalga dönemi not düşülerek kapatıldı.
2. **`ui/scene/tesseract/geometry.ts` (SAF, 10 test):** Gerçek 4B hiperküp — 16 köşe
   (±1,±1,±1,±1), w=+1 dış küp / w=−1 iç küp, perspektif bölme `f=K/(K−w)` (K=3 → 2:1).
   XW düzleminde hiper-dönüş (salınım ±0.38 rad — küpler kimlik değiştirmez), iç köşelerden
   çekirdeğe 8 spoke, spoke üzerinde %55'te 8 violet kapı boncuğu, origin'de çekirdek.
   40 kenar; köprü/spoke uçları merkeze-doğru (a→b) sıralı — converge dir=+1 ile akar.
3. **`ui/scene/tesseract/pulses.ts` (SAF, rng enjekte, 9 test):** synapse (iç+spoke; LLM/ajan)
   + energy (dış+köprü; GPU) + converge türleri. Oran-birikimli doğum (atım/sn), swap-pop
   emeklilik, ÖNCE hareket SONRA doğum (aynı adımda doğup emekli olmaz — ilk test turunda
   yakalanan hata buydu). `fireConverge`: 8 köprü içeri + 8 spoke gecikmeli (t=−0.5) merkeze;
   spoke varışı = coreHit. MAX_PULSES 240, HARD_CAP 320 (doygunsa salvo atlanır).
4. **`ui/scene/TesseractScene.tsx`:** R3F bileşeni. Draw call bütçesi ~11: instanced silindir
   ×3 (bakır 20 / iç 12 / spoke 8) + instanced küre ×3 (düğümler) + çekirdek + 2 sprite +
   2 Points. "Şaşırt beni" ekstraları:
   - **Komet kuyruklu atımlar** — baş + 3 kuyruk noktası, sin-zarfı parlaklık.
   - **Düğüm haleleri** — additive Points, düzlem enerjisiyle parlar (sahte bloom, bağımlılıksız).
   - **Çekirdekte GERÇEK point-light** — patlamada bakır iskelet İÇERİDEN aydınlanır.
   - **Şok-dalgası halkası** — converge varışında dışa genişleyip sönen halka sprite'ı.
   - **Kalp atışı** — çekirdek ölçeği aktiviteyle hızlanan nabız.
   - **İmleç parallax'ı** — ±~0.1 rad derinlik ipucu (iç/dış küp ayrımı okunur).
   - Hiper-dönüş hızı canlılık + çekirdek enerjisiyle artar ("görev bitti = boyut değiştirme").
   Mood iç ağı giyer (idle cyan → executing magenta → awaiting amber → error kırmızı → offline
   sönük %22). VRAM → innerSwell; nefes (grup ölçeği) korundu; yumuşatma RISE/FALL_TAU miras.
5. **Store:** `lastCompletedAt` (agent.run.completed + chat.completed'da damgalanır) → converge
   tetikleyicisi; `lastErrorAt` da tetikler (kritik an). Protokol DEĞİŞMEDİ (mevcut olaylar).
6. **Silinen:** `wave-field.ts` + testi (git geçmişinde; dilim 7'nin dalga modeli tesseract'ta
   anlamını atımlarda buldu).

### Doğrulama

`pnpm build` ✓ · `pnpm test` 212/212 ✓ (203−11 wave-field +10 geometri +9 atım +1 store)
· `pnpm lint` ✓. Üç.js 0.185 fiziksel ışık birimlerine göre yoğunluklar ayarlandı.

### Kullanıcıya kalan

- **Canlı görsel onay:** daemon çalışırken proje kökünden
  `pnpm --filter @symphony/desktop desktop:dev`. Beklenen: bakır tesseract yavaş döner,
  iç ağ tek tük kıvılcımlanır (idle); qwen koşusu → bakırda ağır enerji korları; sohbet/ajan →
  iç ağda hızlı atımlar; tur/koşu bitince converge + çekirdek patlaması + halka; hata → kırmızı.
- İnce ayar: `TesseractScene.tsx` üst sabitleri (boyutlar, TRAIL, ışık katsayıları) +
  `pulses.ts` hız/oran sabitleri.

## Dilim 8b: Sinematik revizyon ("çok basit" geri bildirimi üzerine)

Kullanıcı ilk tesseract'ı sade buldu: "katmanlı küp yapısı, düzlemselden çok estetik ve
fütüristik, şaşırt beni, uç noktalarına kadar kullan". Ayrıca `Yeni klasör/cmdhata.PNG` ile
desktop:dev hatası geldi — teşhis: komut ev dizininden çalıştırılmış (pnpm 468 projelik sahte
workspace bulup ön-install'da patlıyor); çözüm proje kökünden çalıştırmak (bilinen tuzak).

Sinematik revizyonun içeriği (UI-only, protokol değişmedi, YENİ PAKET YOK):
1. **Üç kademeli küp:** kapı boncukları → tam DERİN küp (iç×0.48; 12 kenar + 8 bağ).
   25 düğüm / 60 kenar. Converge 3 kademeli şelale: köprü(t=0) → bağ(−0.5) → spoke(−1.0).
2. **Gerçek bloom:** EffectComposer+UnrealBloomPass+OutputPass (three/examples/jsm — three'nin
   kendi addon'u). Sahne kendi atmosferini çizer: #05060a zemin + 380 yıldız (göz kırpan) +
   3 nebula lekesi. Canvas artık şeffaf değil (bloom şeffaf arka planla siyahlaşırdı).
3. **GLSL akış shader'ı** (instancing-uyumlu ShaderMaterial): cyan/violet tüplerin içinde
   merkeze akan enerji bantları; hız/parlaklık aktivite+çekirdek enerjisiyle.
4. **Jiroskop halkaları ×3** (bakır=GPU, cyan=LLM, violet=çekirdek) + **220 veri zerresi**
   (3 aile, eğik yörüngeler, karşı yönde süzülen grup) + **sinematik kamera** (aktiviteyle
   yaklaşır, sürekli hafif süzülür) + HUD köşe braketleri + `SYMPHONY // LIVING CORE` etiketi.
5. Sahne 300→380px; TASARIM.md §2 güncellendi.

Testler: 211/211 (geometri testleri derin küpe göre yeniden yazıldı). build/lint temiz.
Bloom/parlaklık ince ayarı: `TesseractScene.tsx` BLOOM_* sabitleri + bölüm 11 katsayıları.

### Sıradaki iş (değişmedi)

Kullanıcı öncelik listesi: 1) oturum sürekliliği (DURUM.md'de adım adım plan hazır) →
2) birleşik sohbet-agent → 3) uzun-dönem hafıza/arşiv.

---

<!-- Ayni gun paralel worktree oturumu (Opus/Sonnet) - Oturum 15 merge'iyle tek dosyada birlestirildi -->

# Oturum 14 — 2026-07-08 (Opus 4.8)

## Dilim 1: Oturum sürekliliği — TUI "önceki sohbete devam et" (BİTTİ ve testli)

Kullanıcının #1 önceliği. Kaldığımız yerden (DURUM.md'deki adım adım plan) devam edildi; plan
birebir uygulandı.

### Hedef
TUI'de her açılışta yeni sessionId üretiliyordu → geçmiş görmezden geliniyordu. Artık son sohbet
sürdürülebilir; yerel model (qwen) önceki konuşmanın bağlamını görür.

### Neden protokol/daemon değişmedi
- Geçmiş ZATEN REST'te: `GET /api/history/sessions?limit=N` + `/api/history/sessions/:id`
  (Faz 2, Bearer token'lı; `/api/health` dışı uçlar auth ister).
- Daemon sessionId'ye **REPLACE** semantiğiyle yazıyor (`store.ts`: session'ın mesajlarını sil +
  gelen tüm diziyi yeniden ekle). Yani eski sessionId'yi yeniden kullanıp tam mesaj dizisini
  yeniden göndermek yeterli → çiftleme yok. `chat.start`'a alan eklenmedi.

### Değişiklikler
1. **`cli/src/client/daemon-client.ts`** — `DaemonClient`'a REST geçmiş metotları:
   - `listSessions(limit=50)` → `HistorySessionSummary[]` (yeni→eski).
   - `sessionDetail(id)` → `HistorySessionDetailResponse | null` (404→null).
   - Ortak `getHistory(path)`: Bearer token + `AbortSignal.timeout`; 404→null, diğer hata fırlatır.
     Cevaplar shared zod şemalarıyla doğrulanır (kural 1). Port+token WS'ten zaten elde.
2. **`cli/src/tui/app.tsx`** — yeni `ChatFlow` bileşeni (dışa aktarıldı, test için):
   - `resumeModel` = önceki oturumun provider/model'ine uyan mevcut model (yoksa null).
   - `canResume` (kayıtlı sohbet + model mevcut) ise açılış durumu `resume-choice`, değilse `model-choice`.
   - Devam seçilince `loading` → `sessionDetail` → `ready` (model sabit + mesajlar tohum). system rolü
     filtrelenir (yalnız user/assistant geçmişe). Hata/detay-yok → `model-choice`'a düşer.
   - `runTui` açılışta `listSessions(1)` çeker (`.catch(() => [])` → geçmiş yoksa/başarısızsa sessiz,
     "devam et" gizlenir). `App`'e `lastSession` prop'u eklendi; eski `model` state'i `ChatFlow`'a taşındı.
3. **`cli/src/tui/resume-picker.tsx`** (YENİ) — "Yeni sohbet / Önceki sohbete devam et" seçici
   (mode/model picker deseni: ↑/↓+Enter; devam satırı provider/model · N mesaj · "başlık" gösterir).
4. **`cli/src/tui/chat.tsx`** — opsiyonel `initialSessionId?` + `initialHistory?` prop'ları; state/ref
   bunlarla tohumlanır (yoksa bugünkü davranış). `HistoryEntry` arayüzü dışa aktarıldı.

### Test (203 → 209; 208 geçer)
- `resume-picker.test.tsx` (3): seçenekleri+özeti listeler; Enter→new; ↓+Enter→continue.
- `chat-flow.test.tsx` (3, entegrasyon): devam→eski mesajlar render + yeni mesaj ESKİ sessionId ile +
  bağlam yeniden gönderilir (2 eski + 1 yeni); yeni→YENİ uuid; geçmiş yoksa doğrudan model picker.
- **Not:** ink useInput aşağı-ok testte `String.fromCharCode(27,91,66)` (ESC [ B) ile yazılır; düz
  `"[B"` bu ink sürümünde kaydolmadı (model-picker.test kanonik `[B` kullanıyor).
- **`welcome.test` bu çalıştırmada başarısız ama kod değişikliğiyle İLGİSİZ:** ana checkout'ta da
  (değişiklik olmadan) aynı şekilde başarısız — bu bg-ortamının TTY'siz stdout genişliğinde ink logoyu
  sararak `toContain` kırıyor. Gerçek terminalde geçer (kullanıcının makinesinde 203/203 geçmişti).

### Kapsam v1 / v2'ye ertelenenler
- v1: yalnız SON sohbet + modeli hâlâ mevcutsa. Model artık yoksa "devam et" gizlenir.
- v2: tam oturum tarayıcısı (liste/arama/seç), model değişmişse devam, başlık düzenleme.

### Doğrulama durumu
build ✅ · test ✅ (6 yeni test geçer; welcome ortamsal) · lint ✅. **Canlı TTY doğrulaması KULLANICIYA:**
terminalde `symphony` → Sohbet → "önceki sohbete devam et" → qwen önceki bağlamı hatırlıyor mu?
(global CLI junction → `pnpm build` sonrası anında yansır; daemon restart gerekmez — history REST'ten.)

### İş akışı notu
Bu oturum bir git worktree'de (`worktree-oturum-surekliligi`) yürütüldü; değişiklikler taslak PR olarak
açıldı. Ana dala (main) doğrudan yazılmadı.

## Dilim 2: Birleşik sohbet-agent modu — ADR-012 + Dilim 2.1 (akış) BİTTİ

Kullanıcı "roadmap'i incele, devam edelim" dedi → sıradaki iş #2 (birleşik sohbet-agent modu).
Mimari ADR gerektiriyordu; kullanıcıya sorup **Seçenek A (konuşmalı motor) + akışlı (streamText)** onayı alındı.

### Tasarım (ADR-012 + PROTOKOL.md)
- İki yol (chat.start akışlı/araçsız vs agent.start araçlı/izinli/akışsız/tek-seferlik) **konuşmalı
  motorla** birleşir: konuşma = tamamlanınca `finish` yerine `awaiting_user`'a park olan çok-turlu
  agent koşusu; sonraki tur `agent.say`. Düz sohbet = araçsız "asistan" agent'ı. İzin kapısı/jail/
  araç döngüsü TEK yerde (engine) kalır → B (chat'e araç ekle: kapıyı çatallar) ve C (yeni converse.*:
  çoğaltma) reddedildi.
- PROTOKOL.md güncellendi (ADDITIVE, v1 korunur): `agent.delta` olayı, `awaiting_user` durumu,
  `agent.say` isteği, `agent.start.conversational?`. Dilimler: 2.1 akış → 2.2 çok-tur → 2.3 birleşik TUI.

### Dilim 2.1 — akış (streamText + agent.delta) — kod BİTTİ ve testli
- `shared/events.ts`: `AgentDeltaPayloadSchema {runId,text}` + map girdisi.
- `engine.ts`: `generateText`→`streamText`. Metin `for await (result.textStream)` ile tüketilip her
  parça `agent.delta` yayılır; sonra `await result.{response,usage,providerMetadata,text,toolCalls}`.
  Tool loop/izin/jail/finish/telemetri AYNI. **daemon.ts değişmedi** (bus otomatik yayar).
- Test mock'ları (`engine.test.ts` + `daemon-agent.test.ts`): `doGenerate`→`doStream`; scripted
  `turn()`→AI SDK v3 stream part'ları (`scriptToStream`). **Part şekilleri tahmin edilmedi**:
  `@ai-sdk/provider@4.0.1` `LanguageModelV3StreamPart` tiplerinden birebir okundu (stream-start,
  text-start/delta/end, tool-call{toolCallId,toolName,input}, finish{usage,finishReason}).
- `agent-run.tsx`: `agent.delta`→`streaming` state (green canlı render); araç başlayınca + run
  bitince temizlenir. +1 test.
- **Doğrulama:** build ✅ · test ✅ (engine 11 + daemon-agent 4 + agent-run delta; 209 geçer, welcome
  ortamsal) · lint ✅. Güvenlik-kritik kabul testleri (izin/jail/deny) streamText sonrası YEŞİL kaldı.
- **Canlı doğrulama kullanıcıya:** `symphony` → Agent → bir görev; asistan cevabı artık token-token akmalı.

### Sırada (sonraki oturum, DURUM.md Dilim 2 planında adım adım)
2.2 `awaiting_user` + `agent.say` + `conversational` (motor park/devam + TUI çok-tur) →
2.3 birleşik TUI (varsayılan asistan agent'ı; Sohbet/Agent tek konuşma yüzeyinde).
