# Oturum 12 — 2026-07-07 (Opus 4.8)

## Faz 4 dilim 4: Model panosu (token/maliyet)

**Hedef:** Dashboard'a model başına token/maliyet göstergesi. DURUM.md "Sıradaki adım" #2.

**Karar (mimari):** Bu dilim **yalnız token/maliyet** — VRAM bilerek ertelendi. Gerekçe:
`usage.updated` + `usage.query` protokolde zaten VAR ve daemon yayıyor → token/maliyet **sıfır
protokol değişikliği** gerektiren temiz dikey dilim. VRAM ise protokolde YOK (`hardware`/`vram`
grep boş) → PROTOKOL.md + shared + daemon yayını gerektirir; ikisini karıştırmak CLAUDE.md
"dikey dilim" ve "önce PROTOKOL.md" kurallarını bozardı. VRAM'i kendi dilimine bıraktım (DURUM
"Sıradaki adım" #1'e adım adım yazıldı, ucuz model takip edebilsin).

**Keşif (mevcut durum):**
- `store.ts` `usage.updated`'ı default'ta yok sayıyordu; UI hiç `usage.query` sormuyordu.
- `usage.updated.totals` = provider+model **kümülatif** (daemon.ts:215 `store.usageTotals(p,m)`);
  `deltaTokens/deltaCostUsd` = o turun artışı. → girdiyi totals'la DEĞİŞTİR (çift sayma).
- `usage.query.ok` = `{rows:[{key,in,out,cost}], totals}`; groupBy "model" → key=model.

**Yapılan:**
- `ui/src/store.ts`: `usageTotals`/`usageByModel`(maliyete azalan)/`sessionTokens`/`sessionCostUsd`.
  `usage.updated` → `upsertModelUsage` + genel toplamı yeniden hesap + oturum sayacı.
  `usage.query.ok` → seed. `applySnapshot` oturum sayaçlarını sıfırlar (tüm-zaman dökümü kalır).
- `ui/src/daemon/client.ts`: `queryUsage()` (hello.ok sonrası). Cevap hello-dışı replyTo taşır →
  `handleEvent`'e düşer, id korelasyonu gerekmez (yalnız dashboard usage.query yolluyor).
- `ui/src/App.tsx`: "Model panosu" bölümü — `ModelBoard` + `Metric` + `fmtTokens`/`fmtCost`.
  4 özet metrik + model başına cyan→magenta orantılı çubuk.
- `ui/src/index.css`: `.usage/.metric/.models/.model-bar` (marka paleti).
- `ui/src/store.test.ts`: +3 test (seed / güncelleme-çift-saymaz+oturum / snapshot-sıfırlar).

**Doğrulama:** `pnpm test` 168/168 ✅ (165→168) · `pnpm lint` temiz ✅ · `pnpm build` 4/4 ✅
(tsc strict geçti). Bundle 1.14MB uyarısı three.js'ten (önceden bilinen, masaüstü için sorun değil).
**Görsel doğrulama kullanıcıya** — Bash'ten DOM/panel görülemez; `pnpm --filter @symphony/desktop
desktop:dev` (proje kökünden!) ile pencerede izlenir, daemon çalışıyor olmalı (usage için önce
sohbet/agent koşusu üret ki panoda satır belirsin).

**Sıradaki:** VRAM protokol dilimi (DURUM #1) — önce PROTOKOL.md.

## Faz 4 dilim 5: Donanım vitalleri → Yaşayan Küre (GPU/VRAM/ısı)

**Tetik:** Kullanıcı geri bildirimi — küre "çok sade"; renk/nefes/atımın GPU gibi gerçek
parametrelerle sürülmesini istedi (özellikle yerel model için GPU kullanımı). Bu tam olarak
bir önceki dilimde ertelediğim VRAM protokol dilimiydi → küreye bağladık.

**Karar:** Yalnız GERÇEK telemetri (GPU util/VRAM/sıcaklık). Ölçemediğim "Claude cache" gibi
sahte metrik uydurmadım — TASARIM ilkesi "her hareketin gerçek anlamı var". Bulut tarafı zaten
mood + Model panosuyla yansıyor. GPU'yu kürenin fiziksel katmanı yaptım; mood katmanı korunuyor.

**Yapılan (kural 1 sırasıyla — protokol kutsal):**
- PROTOKOL.md → `hardware.updated {gpus:[…], sampledAt}` (periyodik + hello sonrası son örnek).
- `shared/events.ts`: `GpuSampleSchema` + `HardwareUpdatedPayloadSchema` + kayıt + tip dışa aktarım.
  PROTOCOL_VERSION değişmedi (katkı ekleme; eski istemci bilinmeyen olayı düşürür).
- `core/router/hardware.ts`: `sampleGpus()` + SAF `parseGpuCsv()` (index,name,util,memTotal,
  memUsed,temp; [N/A]→null, util clamp 0..100). `detectVramGb` korundu.
- `core/server/daemon.ts`: 2sn poll (`sampleHardware` opsiyonu, varsayılan true) → yayın + yeni
  bağlanana son örnek + `close()` temizliği. `DaemonOptions.sampleHardware=false` → 2 test daemonu
  (yoksa gerçek nvidia-smi + yayın, `request()`'in `ws.once` mantığını bozup testleri kırardı).
- `ui/store.ts`: `gpus` + `hardware.updated` (applySnapshot bayatı temizler).
- `ui/scene/hardware-vitals.ts` (SAF): `deriveGpuVitals` — en yoğun GPU birincil; load=util/100,
  heat=sıcaklık normalize (TEMP_MIN 35 / MAX 84) ya da sıcaklık yoksa load, memPct.
- `ui/scene/LivingScene.tsx`: küre = mood + fiziksel katman. Yük→dönüş/strain nabzı + sağ-üste
  yaslanma (position lerp); ısı→renk sıcak lerp (WARM #ff5a36, heat*0.7); VRAM→swell. Sağ-üst
  `GpuGauge` HUD (util%/GB/°C, `heatColor` cyan→amber→kırmızı).
- `index.css`: `.scene-gpu/.gpu-*`.

**Doğrulama:** test **177/177** (168→177: +hardware.test 3, +hardware-vitals.test 5, +store 1) ·
lint temiz · build 4/4 (tsc strict). Canlı: `nvidia-smi --query-gpu=index,name,utilization.gpu,
memory.total,memory.used,temperature.gpu --format=csv,noheader,nounits` → `0, NVIDIA GeForce RTX
4060 Laptop GPU, 0, 8188, 932, 50` — parseGpuCsv formatıyla birebir. **Kürenin görsel tepkisi
kullanıcıya** (desktop:dev; yerel modelle koşu başlat → util/ısı artınca küre ısınıp hızlanır).

**Not (gelecek):** çok-GPU HUD, AMD/Apple Silicon, TEMP aralığını karta göre ayarlama (DURUM #2).

## Faz 4 dilim 6: API kapasitesi (rate-limit) + prompt-cache göstergesi

**Tetik:** Kullanıcı "Claude kullanınca limit parametremi görebilir miyim?" + "başka parametre?"
diye sordu. Claude API skill'ini açıp doğru cevabı verdim: Anthropic her cevapta rate-limit'i
header olarak döndürür, AI SDK bunu `response.headers`'ta + cache token'larını
`providerMetadata.anthropic`'te açar → sıfır dış istek. GPU göstergesinin bulut ikizi.

**Statik teyit (uygulamadan önce):** AI SDK v7.0.11 / @ai-sdk/anthropic 4.0.5 d.ts'lerinden
doğrulandı — `LanguageModelResponseMetadata.headers?: Record<string,string>` var;
@ai-sdk/anthropic providerMetadata `cacheReadInputTokens`/`cacheCreationInputTokens` veriyor.

**Yapılan (kural 1 sırasıyla):**
- PROTOKOL.md → `provider.limits` olayı + `usage.updated`'a opsiyonel cache alanları.
- `shared/events.ts` zod (`ProviderLimitsPayload` dışa aktarıldı). PROTOCOL_VERSION değişmedi.
- `core/providers/telemetry.ts` (SAF, testli): `parseRateLimits` (header adı `endsWith` ile
  ek-toleranslı → `anthropic-`/`x-` fark etmez; reset RFC3339 VEYA "kalan sn" → epoch ms;
  exactOptionalPropertyTypes için tanımlıyken atayan `put` yardımcısı) + `extractCacheTokens`.
- `ChatUsageResult` +`cacheReadTokens?`/`cacheCreationTokens?`/`limits?`. anthropic adapter
  `Promise.all([usage,response,providerMetadata])` ile doldurur.
- İKİ yol: daemon runChat (chat) → usage.updated cache + provider.limits; engine (agent) → her
  turda provider.limits yayını, cache `ActiveRunRecord`'da birikip finish'te usage.updated'a girer.
- UI: `store.limits` + `sessionCacheRead/CreationTokens` (applySnapshot sıfırlar). App.tsx
  "API kapasitesi" paneli (`LimitGauge`/`LimitBar`: istek+token kovaları, %kalan→yeşil/amber/kırmızı,
  reset countdown `fmtReset`, 429 uyarısı) + Model panosunda önbellek metriği. index.css `.limit*`.
- Test: `telemetry.test` (5) + store (2). 177→**185**. lint+build temiz (tsc strict geçti).

**Kapsam/dürüstlük:** Rate-limit yalnız Anthropic (diğer sağlayıcılar kendi header adları — sonra).
Claude.ai abonelik kotası API'den okunamaz → EKLENMEDİ. **Canlı header teyidi kullanıcıya** —
paid API çağrısı gerektirdiğinden otomatik probe yapmadım; `desktop:dev`'de bir Claude turu
çalışınca panel dolar (parser ek-toleranslı, dolması beklenir; dolmazsa telemetry.ts tek satır).

**Sıradaki:** DURUM #1 — OpenAI/Gemini limitleri + küre "throttle" uyarı nabzı + cache oranı kesin hesap.

## Faz 4 dilim 7: Yaşayan Küre revizyonu — vektörel dalga (kullanıcı ⭐ ÖNCELİK)

**Tetik:** Kullanıcı önceki oturumda küreyi CLI'dan qwen3:8b koşusuyla izleyince (GPU %98/69°C)
mevcut "zorlanma nabzı"nı (ölçek titreşimi) yüksek-frekans kalp atışı gibi buldu; GPU %0→%100 ani
sıçrayınca atım da ani biniyordu. İstek: (1) yumuşat, (2) scale-nabzını kaldır, (3) yük ifadesi
YÜZEY dalgası (ses-dalgası estetiği) olsun, (4) dalga ekran sağ-üste (GPU göstergesine) doğru
ilerleyip keskinleşsin, (5) renk sıcaklığı dalga yönüne gelsin, (6) LLM aktivitesi de dalgayı sürsün.

**Karar (mimari):** Saf, test-edilebilir `ui/scene/wave-field.ts` — dalga alanı matematiği React/
Three'den ayrı (hardware-vitals/mood deseni). 1700 parçacık CPU BufferAttribute güncellemesi kare
başına önemsiz → ShaderMaterial/GLSL YERİNE saf TS (projenin test disiplinine uyar, GLSL test
edilemezdi). Dönüş `points.rotation`'dan alınıp saf fonksiyonda pozisyona PİŞİRİLDİ (Option B) →
odak/dalga yönü world-uzayında sabit kalır (küre dönerken sağ-üst bölge EKRANDA sabit), her şey
tek yerde test edilir. Yön birleştirme: odak = dalga yönü = `normalize(1,1,0.4)` (sağ-üst) → tek
mekanizma kullanıcının 3 isteğini (yön/keskinleşme/renk) birden karşılar. Protokol DEĞİŞMEDİ
(yeni veri yok; mevcut `gpus` + mood).

**Yapılan:**
- `ui/scene/wave-field.ts` (YENİ, SAF): `rotateDir` (Y sonra X, ortonormal) + `focusWeight`
  (`max(0,dot)^p` keskinleşme) + `computeWaveField` (radyal deformasyon `r=R+amp·dalga+bulge`;
  amp = ambient + drive·(odak-dışı taban + odak boostu); ikinci harmonik; per-parçacık renk
  base→warm lerp, ısı×odak+dalga tepesi). Ayar sabitleri dışa aktarıldı (canlı ince ayar için).
- `ui/scene/mood.ts`: `MoodStyle.activity` (0..1) eklendi — GPU'dan BAĞIMSIZ LLM canlılık sürücüsü
  (idle 0 / thinking .45 / executing .75 / awaiting .2 / error .9 / offline 0). Bulut LLM'de GPU
  yükselmez → mood dalgayı sürer.
- `ui/scene/LivingScene.tsx`: strain-nabzı + lean-throb KALDIRILDI (kullanıcının sevmediği jitter).
  Yumuşatma ref'leri `drive`/`heatSmooth` (kare-hızından bağımsız exp lerp; RISE_TAU .55 hızlı-yumuşak
  biner, FALL_TAU 1.4 yavaş söner). `drive = max(gpuLoad, style.activity)`. Dönüş açıları biriktirilip
  `computeWaveField`'a → position+color attribute her kare güncellenir. pointsMaterial `vertexColors`.
  Ölçek = yalnız yumuşak nefes (mood) + VRAM swell (korundu). Attribute'lar useMemo'da referansla
  tutuldu (`geometry.attributes.position` noUncheckedIndexedAccess'te undefined olabilir).
- `docs/TASARIM.md` §2: "yük ifadesi = vektörel dalga" alt-bölümü yazıldı (eski zorlanma-nabzı gerekçe
  + yeni model: yumuşatma, vektörel dalga, yönlü keskinleşme/renk, ortak sürücü).
- `ui/scene/wave-field.test.ts` (YENİ): 11 test (rotateDir birim-koruma/bilinen dönüş; focusWeight
  monotonluk/keskinleşme; computeWaveField: drive0→renk taban, ambient sınırı, drive↑→deformasyon↑,
  odak bölgesi karşıdan çok ısınır, NaN yok/diziboyu korunur).

**Yan düzeltme (ilgisiz, önceden kırık):** `core/router/router.ts:134` tanımsız `localFitsatı`
→ `localFits`. Son commit "otomatik yedek" (SessionEnd hook) yarım kalan değişken adını yakalamış;
7 router/daemon testi ReferenceError ile kırıktı. Düzeltince suite yeşile döndü.

**Doğrulama:** `pnpm test` **196/196** ✅ (185→196: +wave-field 11; router fix'iyle önceki 7 kırık
yeşil) · `pnpm lint` temiz ✅ · `pnpm build` 4/4 (tsc strict) ✅. **Görsel doğrulama kullanıcıya** —
WebGL Bash'ten görülemez; `pnpm --filter @symphony/desktop desktop:dev` (proje kökünden, daemon
çalışırken). UI-only değişiklik → daemon restart GEREKMEZ (Vite HMR yeni sahne kodunu alır; daemon
yalnız GPU örneği verisi sağlar). Yerel modelle koşu (qwen3:8b) → dalga sağ-üste doğru atmalı/ısınmalı;
Claude/Gemini ile sohbet → GPU yükselmese de mood-activity dalgayı sürmeli.

**Sıradaki:** kullanıcı görsel ince ayarı (wave-field ayar sabitleri) sonrası DURUM #1 (throttle nabzı)
veya Şef Paneli zenginleştirme (agent.step.thinking).
