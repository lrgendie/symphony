# Oturum 15 — 2026-07-09 (Fable)

## Fable inceleme raporunun icrası — merge + protokol işaretleri

Kullanıcı: "dünkü raporu bul, incele, oradan başlayalım." Rapor: `rapor/fabelincelemeraporu.md`
(2026-07-08 22:25 — Opus/Sonnet worktree oturumlarının salt-okunur denetimi). İcra sırası
raporun §7'siydi; 1. ve 2. adım bu oturumda bitti.

### ⚠️ Önce yaşanan ders: raporun §3.1 uyarısı BİZZAT gerçekleşti

Bu oturumun İLK yarısında (rapor henüz bulunmadan) Fable, main'deki DURUM.md'ye göre "sıradaki
iş"i — Dilim 1, oturum sürekliliği — main üzerinde SIFIRDAN yazdı (session-picker.tsx +
fetchLatestSession + chat prop'ları + 4 test; 215/215 yeşil). Oysa aynı dilim bir gün önce
worktree'de (Opus, `2b13ea0`) ZATEN bitmişti. Main'in DURUM'u worktree'den habersiz olduğu için
mükerrer iş yapıldı — raporun "yanlış ağaçta açılan oturum yanlış resimle çalışır" cümlesinin
kanıtı. **Mükerrer sürüm silinmedi:** `yedek/dilim1-fable-mukerrer` dalına commit'lendi (4c776e2).
Yaşayan uygulama Opus/Sonnet'inki seçildi (gerekçe: denetimden geçmiş, üzerine 2.1/2.1b inşa
edilmiş; iki sürüm işlevsel olarak eşdeğer — ChatFlow/resume-picker ⇄ session-picker).

### 1. Merge: `worktree-oturum-surekliligi` → main (rapor §4.1 reçetesi)

- `--no-ff` merge; öngörülen 3 çakışma birebir çıktı, reçeteyle çözüldü:
  - `packages/ui/src/store.ts`: İKİSİ DE kaldı — `agent.run.completed` → `removeRun` +
    `clearStream` + `lastCompletedAt = Date.now()` (tesseract converge). Kalan runStreams
    mantığı (delta biriktirme, tool-started/cancelled/snapshot temizlikleri) otomatik birleşti.
  - `memo/DURUM.md`: tek kronolojik anlatı — Oturum 15 bölümü + Fable'ın 8/8b bölümleri +
    Opus'un Dilim 1/2 bölümleri (kaynak notlarıyla). ⭐ liste zaten "1 ✅ / sırada 2.2" diyordu.
  - `memo/oturumlar/2026-07-08.md` (add/add): iki günlük ayraçla tek dosyada art arda.
- Yan kazanım (raporun öngördüğü): agent koşu bitişi artık masaüstünde HEM akış metni HEM
  tesseract converge patlaması üretiyor — iki dalın işi birbirini tamamladı.

### 2. PROTOKOL.md "planlandı" işaretleri (rapor §3.2)

`agent.say` (istek tablosu), `agent.start.conversational` (istek tablosu) ve `awaiting_user`
(§5 durum makinesi) yanına ⏳ "PLANLANDI (Dilim 2.2) — henüz uygulanmadı" notları kondu;
`agent.say` için bugünkü gerçek davranış (`UNKNOWN_TYPE`) da yazıldı. 2.2 bitince işaretler kalkar.

### Doğrulama (birleşik ağaç)

`pnpm build` ✓ · `pnpm test` **37 dosya / 219 test, tümü geçti** (raporun ~219 tahmini tuttu;
welcome.test dahil) ✓ · `pnpm lint` ✓. BAGLAM.md merge sonrası okundu; store/App satırlarına
worktree'nin `runStreams`/`.run-stream` katkısı işlendi (raporun ek dikkat maddesi).

## Dilim 2.2 (aynı oturum, devam): Çok-tur konuşmalı koşu — BİTTİ (219→224 test)

Kullanıcı "devam edelim" dedi → raporun 4. adımı aynı oturumda uygulandı. Ayrıntı DURUM.md'nin
"Dilim 2.2" bölümünde. Önemli teknik kararlar/dersler:
- **Park runLoop İÇİNDE** (promise-gate `waitForUser`): raporun "2.2'nin en kolay gözden kaçacak
  hatası" dediği MCP-kapanır tuzağı yapısal olarak imkânsızlaştı — `finally`'ye yalnız koşu
  sonlanınca iniliyor, messages+MCP turlar arasında canlı.
- **SQLite v4 göçü zorunlu çıktı:** `agent_runs.state` CHECK'i yeni durumu bilmiyordu (testte
  yakalandı). Tablo yeniden kurma + `migrate()`'e FK-kapalı-göç düzeni (yoksa DROP TABLE,
  agent_steps'i CASCADE ile silerdi — gerçek kullanıcı verisi kaybı olurdu).
- **TUI agent modu artık KONUŞMALI** (kullanıcı önceliği #2'nin özü): görev bitince "devam yaz"
  girişi, Esc bitirir. `agent-run.test`'teki birebir payload doğrulaması `conversational: true`
  ile güncellendi.
- **ink test tuzağı:** dış olayla (stdin dışı) mount edilen TextInput'un input aboneliği tek
  tick'te oturmuyor — testte 3 tick beklenir (yorumla işaretli).

## Rapor §5 küçük iyileştirme paketi (aynı gün, model Sonnet 5'e geçti): BİTTİ (224→232 test)

Kullanıcı `/model` ile Sonnet'e geçti; "kaldığımız yerden devam" dedi. Fable'ın önerdiği iş
bölümüne göre (canlı kontroller kullanıcıda, kod işleri modele göre dağıtılmış) rapor §5'in
4 maddesi kullanıcı onaylarını beklemeden paralel kapatıldı:

1. **`DeltaBatcher`** (`core/src/server/delta-batcher.ts`, YENİ, 5 test): `agent.delta`/
   `chat.delta` artık chunk başına değil ~40ms pencerede toplu yayınlanıyor. En kritik ayrıntı:
   flush sırası. `engine.ts`'de flush for-await'in hemen ardından (response await'inden ÖNCE,
   sağlayıcı hatasında bile en son metin kaybolmasın) VE `catch` bloğunun EN BAŞINDA (finally'de
   DEĞİL — finally, catch'teki `finish()`'ten SONRA çalıştığı için terminal olaydan sonraya
   düşerdi, ilk denemede bu hatayı yaptım ve düzelttim). `daemon.ts`'de aynı desen: flush,
   `chat.completed`'dan ÖNCE.
2. **`runStreams` 2000 karakterle sınırlandı** (`ui/store.ts appendStream`, `slice(-2000)`).
3. **"cancelled" zombi satır düzeltildi** — `removeRun` artık cancelled'da da çağrılıyor.
4. **Stream-ortası-hata testi → gerçek bug bulundu.** Önce kaynak okuyarak "textStream error
   part'ı sessizce yutar, response/usage await'i reddeder" diye tahmin ettim; test bu varsayımla
   4sn timeout'a düştü (agent.run.failed hiç gelmedi). Tahmin yerine izole bir Node script'iyle
   (`packages/core/probe-stream-error.mjs`, sonra silindi) gerçek `ai@7.0.11` + `MockLanguageModelV3`
   davranışını ÖLÇTÜM: `result.response`/`result.usage` REDDETMİYOR, `finishReason:"error"` ile
   normal dönüyor. Yani gerçek bir sağlayıcı hatası, kontrolsüz motor tarafından BOŞ bir
   `agent.run.completed` sanılırdı — sessiz veri kaybı. `engine.ts`'e `await result.finishReason
   === "error"` kontrolü eklendi → `AgentError("PROVIDER_STREAM_ERROR", ...)`. Ders: AI SDK gibi
   büyük/minifed harici kütüphanelerin hata semantiğini kaynak okuyarak tahmin etmek yerine küçük
   çalıştırılabilir bir prob ile doğrulamak çok daha güvenilir.

Doğrulama: build + **38 dosya / 232 test** (224→232) + lint temiz. Bu iş kullanıcı canlı
kontrollerinden bağımsız olduğu için onlar beklenmeden bitirildi.

### Sıradaki (raporun §7 icra sırası — güncel)

3. ✅ **Kullanıcı canlı onayları (dört kontrol) — HEPSİ GEÇTİ** (2026-07-09):
   - Eski daemon (PID 8340, bu sabahtan kalma, Dilim 2.2/rapor§5'ten ÖNCEKİ kod) yönetici
     yetkisi gerektirdi — kullanıcı kendi yükseltilmiş cmd'sinden `taskkill /PID 8340 /F` ile
     kapattı (normal kullanıcı oturumundan `Erişim engellendi` hatası alınmıştı — süreç daha
     yüksek bütünlük seviyesinde başlamış olmalı). Taze `dist`'ten yeni daemon başlatıldı
     (PID 2476), temiz açıldı (yarım koşu yok, v4 göçü gerçek `~/.symphony/data/symphony.db`'de
     sorunsuz uygulandı — foreign_key_check hatasız).
   - `desktop:dev` arka planda başlatıldı (Rust derlemesi ~31sn, hatasız) → tesseract sinematik
     hâliyle (dilim 8+8b) dönüyor, kullanıcı onayladı.
   - TUI konuşmalı agent (dilim 2.2): kullanıcı denedi, "gayet güzel çalıştı".
   - TUI "önceki sohbete devam et" (Dilim 1): seçenek çıktı, eski mesajlar yüklendi, qwen
     bağlamı hatırladı — tam onay.
   - **Yan bulgu (Symphony hatası DEĞİL):** kullanıcının TUI cwd varsayılanı System32 çıktı;
     kök neden Win+R→cmd'nin System32'yi cwd yapması (standart Başlat Menüsü kısayolları
     kontrol edildi, hepsinin WorkingDirectory'si `%HOMEDRIVE%%HOMEPATH%` — temiz). Ayrıca
     "masaüstü klasörü düzenleme" görevi için doğru cwd tavsiyesi verildi: `C:\Users\brkn2\Desktop`
     (Public Desktop'a dokunması gerekirse `extraDirs` ile, jail'i `C:\Users`'a genişletmeden).
4. **Dilim 2.3 (birleşik TUI)** ← SIRADAKİ TEK KOD İŞİ, model **Opus**'a geçiyor: varsayılan
   araçsız "asistan" agent'ı + Sohbet/Agent'ın tek konuşma yüzeyinde birleşmesi + ChatFlow
   (Dilim 1) harmanı. ROADMAP kullanıcı önceliği #2'nin son parçası. Yarı tasarım yarı uygulama —
   ADR-012'nin sahibi zaten Opus, bağlamı hazır.

### Oturum sonu canlı durum (devralan model için)

Daemon (PID 2476, port 7770) ve `desktop:dev` (Tauri penceresi) ŞU AN ARKA PLANDA ÇALIŞIYOR —
ikisi de Dilim 2.2 + rapor §5 dahil taze koddan. Opus devraldığında bunlara dokunmasına GEREK
YOK (Dilim 2.3 kod değişikliği yapıp test edecekse yine restart gerekecek — o zaman bu ikisini
kapatıp aynı şekilde yeniden başlatma deseni uygulanır, bkz. yukarıdaki adım).

---

## Fable denetim oturumu (akşam): İnceleme Raporu 2 — YAZILDI

Kullanıcı isteği: merge + 2.2 + §5 paketi + 2.3a/b/c'nin `fabelincelemeraporu.md` deseninde
mimari denetimi (odak: transcript düzleştirme · chat/agent ortak sessions · awaiting_user+MCP ·
finishReason:"error"). Çıktı: **`rapor/fabelincelemeraporu2.md`**. Koda müdahale edilmedi;
#3'e (hafıza/arşiv) BAŞLANMADI (istek gereği).

- **Doğrulama bizzat koşuldu:** build ✓ · 38 dosya / 240 test ✓ · lint ✓.
- **Dört odak da sağlam çıktı** (park runLoop içinde → MCP yapısal çözüm; finishReason
  düzeltmesi + testi örnek; kalıcılık tek kapıdan; transcript düzleştirme UI ile hizalı).
- **Bulunan pürüzler (raporda reçeteli):** §3.1 TUI resume + "Enter→yeni görev" eski oturuma
  sessiz devam (agent-run.tsx:128, initialSessionId reset'te düşmüyor — EN ÖNEMLİ) · §3.2 say
  kullanıcı turu ancak sonraki asistan turunda persist (kayıp penceresi) · §3.3 queued→failed
  geçersiz geçiş (MCP bağlantı hatasında state olayı yayınlanmaz — 2.2 ÖNCESİNDEN kalma) ·
  §3.4 requests.session_id çift anlamlı (chat: oturum / agent: runId) · §4: boşta-park zombi
  sigortası, conversational'sız sessionId yarı-davranışı, eşzamanlı REPLACE yarışı, sessions'ta
  persona kimliği yok, iki küçük kozmetik.

---

## Sonnet devir: rapor2 §3.1-§3.4 düzeltme paketi — TAMAM (244 test)

Model Sonnet 5'e geçirildi (kullanıcı: "hataları kapat, #3'e geçmeden önce ucuz model daha
avantajlı"). Fable'ın DURUM.md'ye yazdığı devir talimatı izlendi — mimari yeniden okunmadı,
yalnız rapor2 §3 + 2 hedef dosya (`agent-run.tsx`, `engine.ts`) açıldı.

1. **§3.1** `agent-run.tsx`: `initialSessionId` state'e alındı, `resetForNewTask` `undefined`'a
   düşürüyor — "yeni görev" artık gerçek yeni oturum. +1 test.
2. **§3.2** `engine.ts`: `persistConversation` iki yeni noktada çağrılıyor (ilk görev metni +
   `say()` sonrası kullanıcı turu) — model turu bitmeden kalıcılaşıyor. +2 test (yeni
   `deferredTurn()`/`deferredStream()` test yardımcıları — stream teste kadar asılı kalır,
   race'siz zamanlama doğrulaması).
3. **§3.3** `engine.ts`: ilk `transition(thinking)` MCP-connect/languageModel'den ÖNCEYE
   taşındı — bağlantı hatasında artık geçerli `thinking→failed` (önceden `queued→failed`
   sessizce reddediliyordu, olay hiç yayınlanmıyordu). +1 test.
4. **§3.4** `engine.ts` `recordTurnUsage`: `requests.session_id` artık `run.runId` değil
   `run.sessionId` — chat.start ile aynı sütun anlamı.
- **Yan bulgu:** §3.3'ün erken transition'ı, §3.2 testlerinden birinin "thinking" SAYISINA
  dayanan senkronizasyonunu bozdu (olay artık MCP-connect'ten önce ateşleniyor) — düzeltildi:
  `adapter.deferred.length > 0` bekleniyor (somut yan etki, olay sayısı değil). Ders: mock
  zamanlama testlerinde olay sayısı yerine somut yan etkiyi beklemek daha sağlam.
- **Doğrulama:** `pnpm build && pnpm test && pnpm lint` temiz — 38 dosya / **244 test**.
- **Sıradaki:** ROADMAP #3 (uzun-dönem hafıza/arşiv) — henüz başlanmadı.

---

## Fable tasarım oturumu (gece): öncelik #3 uzun-dönem hafıza — ADR-013 + M1/M2/M3 planı

Kullanıcı #3'ü başlattı (yalnız tasarım; uygulama Sonnet'e). Kod YAZILMADI. Çıktılar:
- **ADR-013** (`docs/kararlar/KARARLAR.md`): (a) stil/tercih profili ile başla — sıfır yeni
  bağımlılık, tam kullanıcı kontrolü; (b) RAG Bağlam Haritası dilimi ile birlikte tasarlanmak
  üzere ertelendi (aynı embedding altyapısını paylaşacaklar); (c) LoRA süresiz ertelendi.
  **Yazma kısıtı korundu ve güçlendirildi:** canlı `profil.md`'yi yalnız insan yazar; damıtma
  yalnız `profil.taslak.md` üretir (prompt-injection insan onayı kapısında süzülür). Veri katmanı
  DOSYA (kullanıcı-düzenlenebilir, Faz 7 sync ile bedava taşınır). Enjeksiyon tek boru, sunucu
  tarafı: agent=buildSystemPrompt, chat=runChat kopyaya system-önek (kalıcılığa GİRMEZ).
  `memory.enabled` acil kapama anahtarı. Damıtma (M3) YENİ PROTOKOL AÇMADAN salt-okur
  "damitici" agent olarak koşar; gizlilik varsayılanı yerel model (--bulut ile override).
- **PROTOKOL.md §1.1**: M2 REST uçları (`GET/PUT /api/memory`) "planlandı" İŞARETLİ eklendi
  (rapor1 §3.2 dersi: işaretsiz gelecek özellik belgelenmez).
- **DURUM.md**: M1 (çekirdek enjeksiyon, protokolsüz) → M2 (REST+CLI+TUI göstergesi) →
  M3 (damıtıcı agent) adım adım, dosya dosya talimat — Sonnet'in okuma listesi dahil.
- ROADMAP #3 satırına "tasarım tamam" işareti; BAGLAM görev tablosuna hafıza satırı.

---

## Sonnet devir: Dilim M1 (öncelik #3 çekirdek enjeksiyon) — TAMAM (255 test)

Fable'ın ADR-013 + M1/M2/M3 planı üzerine M1 uygulandı (yeniden tasarım yapılmadan,
DURUM.md'deki okuma listesi + adım adım talimat izlendi).

- `paths.ts` (`profileFile`) · YENİ `core/src/memory/profile.ts` (SAF: loadProfile/
  ensureProfileScaffold, 8 test) · `config.ts` (`memory.enabled`) · `engine.ts`
  (`loadMemoryProfile` dep + `buildSystemPrompt` 3. parametre) · `daemon.ts` (açılışta
  iskelet + gerçek loader).
- **Uygulama sırasında gerçek bir hata yakalandı ve düzeltildi:** ADR-013'ün ilk tasarımı
  chat yolunda "provider mesaj kopyasına system-önek ekle" diyordu; canlı testte (daemon.test.ts,
  gerçek daemon + fake Ollama HTTP sunucusu) AI SDK v7'nin `messages`/`prompt` içinde `system`
  rolünü `InvalidPromptError` ile reddettiği ortaya çıktı — engine.ts'in zaten bildiği kısıt
  (`instructions` seçeneği), chat yoluna UYGULANMAMIŞTI. Düzeltme: `ChatStreamRequest.instructions?`
  eklendi, dört adapter da (`anthropic`/`openai`/`google`/`ollama`) bunu `streamText`e iletir.
  ADR-013 Karar 4 bu düzeltmeyi yansıtacak şekilde güncellendi.
- **Test:** 244→**255** (profile 8, engine +2, daemon +1). Daemon testi GERÇEK bir daemon
  başlatıp WS ile chat.start yapıyor, fake-Ollama'nın aldığı HTTP gövdesinde profili doğruluyor,
  `sessionDetail` dökümünde YOK olduğunu doğruluyor — ROADMAP Faz 6 kabul testinin otomatik
  karşılığı. `pnpm build && pnpm test && pnpm lint` temiz.
- **Ders:** "ADR'de yazan tasarım" ile "gerçek kütüphane davranışı" arasındaki uyumsuzluk ancak
  canlı/entegrasyon testiyle yakalandı — birim test (yalnız engine.test.ts mock'u) bunu
  KAÇIRIRDI çünkü mock adapter gerçek AI SDK'nın system-mesaj kısıtını uygulamıyor.
- **Sıradaki:** Dilim M2 (REST + CLI + TUI göstergesi) — DURUM.md'de talimat hazır.
