#!/usr/bin/env node
// symphony CLI — Faz 2'de Ink TUI'ye dönüşecek iskelet.
import { DEFAULT_DAEMON_PORT, PROTOCOL_VERSION } from "@symphony/shared";

console.log("🎼 Symphony v0.1.0 — iskelet sürüm");
console.log(`   protokol: v${PROTOCOL_VERSION} | daemon portu: ${DEFAULT_DAEMON_PORT}`);
console.log("   Model seçici ve sohbet Faz 2'de geliyor (bkz. ROADMAP.md).");
